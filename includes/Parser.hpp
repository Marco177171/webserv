#ifndef PARSER_HPP
# define PARSER_HPP

class Parser {
	public:
		Parser(void);
		Parser(const Parser &source);
		Parser &operator=(const Parser &source);
		~Parser(void);
		// public methods here...
	private:
		// private methods here...
};

#endif